# Extending the system.site configuration.
system.site:
  mapping:
    page:
      mapping:
        site_403_fallback:
          type: path
          label: 'Fallback 403 (access denied) page'
        site_403_memberonly:
          type: path
          label: 'Member-only 403 (access denied) page'
